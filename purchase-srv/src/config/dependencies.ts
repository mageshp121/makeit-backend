import {  cartRepository } from '../libs/app/repository/index'
import { Cart_usecase } from '../libs/usecase';

const useCase:any = {
      Cart_usecase
};
const repository:any={cartRepository}
export default {repository,useCase }